# minhash-dedup-practice

Hands-on practice: implement MinHash + LSH near-duplicate detection from scratch on a 100K-document corpus.

See **CLAUDE.md** for the full phase-based plan (8 phases: corpus → preprocessing → shingling → MinHash → LSH → dedup → evaluation → writeup).

Project status: step zero. All folders exist and are empty.
# minhash-dedup-practice
